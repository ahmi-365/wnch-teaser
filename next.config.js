/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'plus.unsplash.com', 'encrypted-tbn0.gstatic.com', 'media.istockphoto.com'],
      },
}

module.exports = nextConfig
